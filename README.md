
  # Dark Minimal Web Interface

  This is a code bundle for Dark Minimal Web Interface. The original project is available at https://www.figma.com/design/dlsvuUsUE6b0N7Rtk1qyYB/Dark-Minimal-Web-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  